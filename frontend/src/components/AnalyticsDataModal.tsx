'use client';

// analytics removed — no telemetry in this fork
interface AnalyticsDataModalProps {
  isOpen: boolean;
  onClose: () => void;
  onConfirmDisable: () => void;
}

export default function AnalyticsDataModal(_props: AnalyticsDataModalProps) {
  return null;
}
